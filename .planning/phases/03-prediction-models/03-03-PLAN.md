---
phase: 03-prediction-models
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/hermes/models/player_predictor.py
  - tests/models/test_player_predictor.py
autonomous: true
requirements: [PRED-03, PRED-04, PRED-07]

must_haves:
  truths:
    - "For any player in today's games, the system predicts points, rebounds, assists, and 3PM"
    - "Each player prop prediction includes a confidence interval"
    - "Matchup-specific history is incorporated into predictions (player vs opponent defense)"
  artifacts:
    - path: "src/hermes/models/player_predictor.py"
      provides: "Player prop predictions for points, rebounds, assists, 3PM with matchup context"
      contains: "class PlayerPropPredictor"
      exports: ["PlayerPropPredictor"]
    - path: "tests/models/test_player_predictor.py"
      provides: "Tests for player prop predictions"
      min_lines: 80
  key_links:
    - from: "src/hermes/models/player_predictor.py"
      to: "src/hermes/models/base_model.py"
      via: "inherits BasePredictor"
      pattern: "class PlayerPropPredictor.*BasePredictor"
    - from: "src/hermes/models/player_predictor.py"
      to: "src/hermes/data/features/api.py"
      via: "consumes full feature dict including matchup stats"
      pattern: "matchup_avg|matchup_diff"
---

<objective>
Build the player prop prediction model (PRED-03) that predicts points, rebounds, assists, and 3PM for individual players, incorporating matchup-specific analysis (PRED-07) and confidence intervals (PRED-04).

Purpose: Player props are the most granular prediction type. The model uses rolling averages, advanced stats, AND matchup history (player vs specific team defense) to produce prop lines. This is where the matchup features from Phase 2 pay off.
Output: PlayerPropPredictor class handling all four stat types.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prediction-models/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 03-01 -->

From src/hermes/models/base_model.py:
```python
@dataclass
class PredictionResult:
    value: float
    confidence_lower: float
    confidence_upper: float
    metadata: dict

class BasePredictor(ABC):
    def train(self, features: list[dict], targets: list[float]) -> None: ...
    def predict(self, features: dict) -> PredictionResult: ...
    def get_feature_names(self) -> list[str]: ...
```

From src/hermes/data/features/api.py -- get_features() returns dict with these keys:
```
Rolling stats (36 cols): points_avg_5, points_avg_10, points_avg_20, rebounds_avg_5, ..., fg3_pct_avg_5, ...
Advanced: true_shooting_pct, usage_rate, simplified_per
Matchup: matchup_avg_points, matchup_avg_rebounds, matchup_avg_assists, matchup_avg_fg3m,
         matchup_diff_points, matchup_diff_rebounds, matchup_diff_assists, matchup_diff_fg3m,
         matchup_games_played
Team: team_pace, team_offensive_rating, team_defensive_rating, team_rest_days, team_season_win_pct
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlayerPropPredictor -- multi-stat prediction with matchup context</name>
  <files>
    src/hermes/models/player_predictor.py,
    tests/models/test_player_predictor.py
  </files>
  <behavior>
    - Test 1: PlayerPropPredictor("points") trains on feature dicts with points targets and predicts with CI
    - Test 2: PlayerPropPredictor("rebounds") trains and predicts rebounds
    - Test 3: PlayerPropPredictor("assists") trains and predicts assists
    - Test 4: PlayerPropPredictor("fg3m") trains and predicts 3-pointers made
    - Test 5: When matchup_avg_points is high (player historically scores more vs this team), predicted points are higher than baseline
    - Test 6: When matchup_games_played is 0 or None, model still produces valid predictions (falls back to rolling averages)
    - Test 7: predict_all_props(features) returns dict of {stat_name: PredictionResult} for all four stats at once
    - Test 8: Confidence intervals are tighter for players with more games_available_20 (more data = more certainty)
  </behavior>
  <action>
    1. Create `src/hermes/models/player_predictor.py`:
       - `PlayerPropPredictor(BasePredictor)` -- initialized with stat_type ("points", "rebounds", "assists", "fg3m")
       - `PLAYER_FEATURES` dict mapping stat_type to relevant feature subset:
         * points: rolling point avgs, TS%, USG%, matchup_avg_points, matchup_diff_points, team_pace, team_offensive_rating, opp_defensive_rating, minutes_avg_*
         * rebounds: rolling rebound avgs, offensive_rebounds_avg_*, matchup_avg_rebounds, team_pace
         * assists: rolling assist avgs, USG%, matchup_avg_assists, team_pace, team_offensive_rating
         * fg3m: rolling fg3 avgs, fg3_pct_avg_*, matchup_avg_fg3m, team_pace
       - `train()`: Uses sklearn GradientBoostingRegressor (handles nonlinearity in matchup effects). Store residual_std per stat.
       - `predict()`: Returns PredictionResult with value=predicted_stat, CI from residual_std. metadata includes matchup_adjustment (how much matchup shifted prediction from rolling avg baseline).
       - Confidence interval scaling: If games_available_20 < 10, widen CI by factor of sqrt(20/games_available_20) -- less data = more uncertainty.
       - Handle None/NaN matchup features: If matchup_games_played is 0 or None, zero out all matchup features (model uses only rolling/team features).
    2. `predict_all_props(features: dict) -> dict[str, PredictionResult]` convenience method: Creates 4 PlayerPropPredictors (one per stat), trains each (if not already trained -- uses self._models dict), returns all predictions.
       Actually, better design: A `PlayerPropsPredictor` (plural) wrapper class that holds 4 individual predictors internally and exposes predict_all(features) -> dict.
    3. Write thorough tests with synthetic feature dicts. For matchup test: create features where matchup_avg_points is high vs a specific team and verify predicted points exceed the rolling average baseline.
  </action>
  <verify>
    <automated>python -m pytest tests/models/test_player_predictor.py -x -v</automated>
  </verify>
  <done>PlayerPropPredictor predicts points/rebounds/assists/3PM with CIs, matchup features influence predictions, handles missing matchup data gracefully</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/models/ -x -v` -- all model tests pass
- `python -c "from hermes.models.player_predictor import PlayerPropPredictor"` -- imports
- Matchup influence visible in test output (high matchup avg -> higher prediction)
</verification>

<success_criteria>
- PlayerPropPredictor predicts all four stat types with confidence intervals
- Matchup-specific history demonstrably influences predictions (PRED-07)
- Missing matchup data handled gracefully (no crash, falls back to rolling avgs)
- Confidence intervals scale with data availability
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-prediction-models/03-03-SUMMARY.md`
</output>
