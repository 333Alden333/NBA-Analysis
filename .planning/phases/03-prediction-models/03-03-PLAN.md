---
phase: 03-prediction-models
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/hermes/models/player_predictor.py
  - tests/models/test_player_predictor.py
autonomous: true
requirements: [PRED-03, PRED-04, PRED-07]

must_haves:
  truths:
    - "For any player in today's games, the system predicts points, rebounds, assists, and 3PM"
    - "Each player prop prediction includes a quantile regression confidence interval"
    - "Matchup-specific history is incorporated into predictions (player vs opponent defense)"
    - "3PM prediction uses fg3_pct_avg rolling stats and matchup_avg_fg_pct (NOT matchup_avg_fg3m which does not exist)"
  artifacts:
    - path: "src/hermes/models/player_predictor.py"
      provides: "Player prop predictions for points, rebounds, assists, 3PM with matchup context"
      contains: "class PlayerPropPredictor"
      exports: ["PlayerPropPredictor"]
    - path: "tests/models/test_player_predictor.py"
      provides: "Tests for player prop predictions"
      min_lines: 80
  key_links:
    - from: "src/hermes/models/player_predictor.py"
      to: "src/hermes/models/base_model.py"
      via: "inherits BasePredictor"
      pattern: "class PlayerPropPredictor.*BasePredictor"
    - from: "src/hermes/models/player_predictor.py"
      to: "src/hermes/data/features/api.py"
      via: "consumes full feature dict including matchup stats"
      pattern: "matchup_avg_points|matchup_avg_fg_pct|fg3_pct_avg"
---

<objective>
Build the player prop prediction model (PRED-03) that predicts points, rebounds, assists, and 3PM for individual players, incorporating matchup-specific analysis (PRED-07) and quantile regression confidence intervals (PRED-04).

Purpose: Player props are the most granular prediction type. The model uses rolling averages, advanced stats, AND matchup history to produce prop lines. Uses quantile regression (same as TotalsPredictor) for proper confidence intervals.
Output: PlayerPropPredictor class handling all four stat types with matchup context.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prediction-models/03-01-SUMMARY.md
@.planning/phases/03-prediction-models/03-RESEARCH.md

<interfaces>
<!-- From Plan 03-01 -->

From src/hermes/models/base_model.py:
```python
@dataclass
class PredictionResult:
    value: float
    confidence_lower: float
    confidence_upper: float
    metadata: dict

class BasePredictor(ABC):
    def train(self, features: list[dict], targets: list[float]) -> None: ...
    def predict(self, features: dict) -> PredictionResult: ...
    def get_feature_names(self) -> list[str]: ...
    @staticmethod
    def features_to_array(features: dict, feature_names: list[str]) -> list[float]: ...
```

From src/hermes/data/features/api.py -- ACTUAL feature dict keys available:

Rolling stats (36 columns):
```
points_avg_5, points_avg_10, points_avg_20
rebounds_avg_5, rebounds_avg_10, rebounds_avg_20
assists_avg_5, assists_avg_10, assists_avg_20
steals_avg_5, steals_avg_10, steals_avg_20
blocks_avg_5, blocks_avg_10, blocks_avg_20
turnovers_avg_5, turnovers_avg_10, turnovers_avg_20
fg_pct_avg_5, fg_pct_avg_10, fg_pct_avg_20
fg3_pct_avg_5, fg3_pct_avg_10, fg3_pct_avg_20
ft_pct_avg_5, ft_pct_avg_10, ft_pct_avg_20
minutes_avg_5, minutes_avg_10, minutes_avg_20
plus_minus_avg_5, plus_minus_avg_10, plus_minus_avg_20
offensive_rebounds_avg_5, offensive_rebounds_avg_10, offensive_rebounds_avg_20
games_available_5, games_available_10, games_available_20
```

Advanced stats:
```
true_shooting_pct, usage_rate, simplified_per
```

Matchup stats (ACTUAL columns -- NO fg3m-specific matchup tracking):
```
matchup_games_played
matchup_avg_points, matchup_avg_rebounds, matchup_avg_assists
matchup_avg_fg_pct, matchup_avg_plus_minus
matchup_diff_points, matchup_diff_rebounds, matchup_diff_assists
matchup_diff_fg_pct, matchup_diff_plus_minus
```

Team features:
```
team_pace, team_offensive_rating, team_defensive_rating
team_rest_days, team_possessions, team_opponent_possessions, team_season_win_pct
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlayerPropPredictor -- multi-stat prediction with matchup context and quantile CI</name>
  <files>
    src/hermes/models/player_predictor.py,
    tests/models/test_player_predictor.py
  </files>
  <behavior>
    - Test 1: PlayerPropPredictor("points") trains on feature dicts with points targets and predicts with quantile CI
    - Test 2: PlayerPropPredictor("rebounds") trains and predicts rebounds
    - Test 3: PlayerPropPredictor("assists") trains and predicts assists
    - Test 4: PlayerPropPredictor("fg3m") trains and predicts 3-pointers made
    - Test 5: When matchup_avg_points is high (player historically scores more vs this team), predicted points are higher than baseline
    - Test 6: When matchup_games_played is 0 or None, model still produces valid predictions (falls back to rolling averages)
    - Test 7: predict_all_props(features) returns dict of {stat_name: PredictionResult} for all four stats at once
    - Test 8: Confidence intervals are wider when games_available_20 is low (less data = more uncertainty)
  </behavior>
  <action>
    1. Create `src/hermes/models/player_predictor.py`:

       - `PlayerPropPredictor(BasePredictor)` -- initialized with stat_type ("points", "rebounds", "assists", "fg3m")

       - `PLAYER_FEATURES` dict mapping stat_type to relevant feature names. CRITICAL: Use only features that ACTUALLY EXIST in the feature dict:
         * points: ["points_avg_5", "points_avg_10", "points_avg_20", "true_shooting_pct", "usage_rate", "minutes_avg_5", "minutes_avg_10", "matchup_avg_points", "matchup_diff_points", "matchup_games_played", "team_pace", "team_offensive_rating", "games_available_20"]
         * rebounds: ["rebounds_avg_5", "rebounds_avg_10", "rebounds_avg_20", "offensive_rebounds_avg_5", "offensive_rebounds_avg_10", "minutes_avg_5", "minutes_avg_10", "matchup_avg_rebounds", "matchup_diff_rebounds", "matchup_games_played", "team_pace", "games_available_20"]
         * assists: ["assists_avg_5", "assists_avg_10", "assists_avg_20", "usage_rate", "minutes_avg_5", "minutes_avg_10", "matchup_avg_assists", "matchup_diff_assists", "matchup_games_played", "team_pace", "team_offensive_rating", "games_available_20"]
         * fg3m: ["fg3_pct_avg_5", "fg3_pct_avg_10", "fg3_pct_avg_20", "fg_pct_avg_5", "minutes_avg_5", "minutes_avg_10", "matchup_avg_fg_pct", "matchup_diff_fg_pct", "matchup_games_played", "team_pace", "games_available_20"]
           NOTE: There is NO matchup_avg_fg3m or matchup_diff_fg3m in the MatchupStats model. Use fg3_pct rolling averages and matchup_avg_fg_pct (overall FG% correlates with 3P%) as proxy.

       - `train(features, targets)`: Uses THREE GradientBoostingRegressors with quantile loss (same pattern as TotalsPredictor):
         * model_lower: alpha=0.05
         * model_median: alpha=0.5
         * model_upper: alpha=0.95
         Common params: learning_rate=0.05, n_estimators=200, max_depth=2, min_samples_leaf=9, min_samples_split=9

       - `predict(features)`: Returns PredictionResult where:
         * value = median model prediction
         * confidence_lower = lower model prediction (5th percentile)
         * confidence_upper = upper model prediction (95th percentile)
         * metadata = {"stat_type": self.stat_type, "matchup_games": features.get("matchup_games_played", 0), "interval_pct": 90}
         * CI widening: If games_available_20 < 10 and games_available_20 > 0, multiply interval width by sqrt(20 / games_available_20). This accounts for less certainty with sparse data.

       - Handle None/NaN matchup features: If matchup_games_played is 0 or None, zero out all matchup_* features (model uses only rolling/team features). features_to_array already handles None->0.0.

    2. `PlayerPropsPredictor` (plural) wrapper class:
       - Holds dict of {stat_type: PlayerPropPredictor} internally
       - `train_all(features_list, targets_dict)`: trains each stat predictor. targets_dict maps stat_type to list of targets.
       - `predict_all(features) -> dict[str, PredictionResult]`: runs all 4 predictors on same feature dict.
       - `save(path)` / `load(path)`: serializes all 4 models together.

    3. Write thorough tests with synthetic feature dicts. For matchup test: create two feature sets identical except matchup_avg_points is 30 in one and 10 in another. After training on correlated data, verify the high-matchup prediction exceeds the low-matchup prediction.

    4. Use at least 50 synthetic samples per stat type for GBR training.
  </action>
  <verify>
    <automated>python -m pytest tests/models/test_player_predictor.py -x -v</automated>
  </verify>
  <done>PlayerPropPredictor predicts points/rebounds/assists/3PM with quantile regression CIs, matchup features influence predictions, handles missing matchup data gracefully, fg3m uses fg3_pct rolling stats (not nonexistent matchup_avg_fg3m)</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/models/ -x -v` -- all model tests pass
- `python -c "from hermes.models.player_predictor import PlayerPropPredictor, PlayerPropsPredictor"` -- imports
- Matchup influence visible in test output (high matchup avg -> higher prediction)
- No references to matchup_avg_fg3m or matchup_diff_fg3m anywhere in source code
</verification>

<success_criteria>
- PlayerPropPredictor predicts all four stat types with quantile regression confidence intervals
- Matchup-specific history demonstrably influences predictions (PRED-07)
- 3PM model uses fg3_pct_avg_* and matchup_avg_fg_pct (NOT nonexistent matchup_avg_fg3m)
- Missing matchup data handled gracefully (no crash, falls back to rolling avgs)
- Confidence intervals widen with sparse data (games_available_20 < 10)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-prediction-models/03-03-SUMMARY.md`
</output>
